<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-2 fixed-top">
    <%= link_to image_tag("Gossip-icon.png", alt: "Logo", height: 40), root_path, class: "navbar-brand" %>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse p-2" id="navbarNav">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
                <%= link_to "Accueil", root_path, class: "nav-link active", "aria-current"=>"page"%>
            </li>
            <li class="nav-item">
                <%= link_to "Notre équipe", teams_path, class: "nav-link"%>
            </li>
            <li class="nav-item">
                <%= link_to "Nous contacter", new_contact_path, class: "nav-link"%>
            </li>
            <!-- Condition for logout only if current_user connected -->
            <% if current_user %>
                <li class="nav-item">
                    <%= link_to "Deconnexion", session_path(session.id), data: { turbo_method: :delete }, class: "nav-link"%>
                </li>
                <li class="nav-item">
                    <span class="nav-link text-muted">Connecté en tant que <%= current_user.first_name %></span>
                </li>
            <!-- Menu login/signin only if user is not connected -->    
            <% else %>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    S'inscrire / Se connecter </a>
                    <ul class="dropdown-menu">
                        <li><%= link_to "S'inscrire", new_user_path, class: "dropdown-item" %></li>
                        <li><%= link_to "Se connecter", new_session_path, class: "dropdown-item" %></li>
                    </ul>
                </li>
            <% end %>
        </ul>
        <form class="d-flex">
            <input class="form-control me-2" type="search" placeholder="Rechercher" aria-label="Search">
            <button class="btn btn-outline-light" type="submit">Rechercher</button>
        </form>
    </div>
</nav>