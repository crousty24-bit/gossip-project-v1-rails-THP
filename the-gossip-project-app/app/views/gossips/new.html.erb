<form action="<%= gossips_path %>" method="POST" class="container py-5">

  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">

  <div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
      <div class="card shadow-lg border-0 rounded-4">
        <div class="card-body p-4">

          <h2 class="text-center mb-4 fw-bold">Balance ton potin 🫢</h2>

          <div class="mb-3">
            <label class="form-label fw-semibold">Titre</label>
            <input type="text" name="title" class="form-control form-control-lg" placeholder="Un titre croustillant...">
          </div>

          <div class="mb-4">
            <label class="form-label fw-semibold">Ton potin</label>
            <textarea name="content" rows="5" class="form-control form-control-lg" placeholder="Raconte tout 👀"></textarea>
          </div>

          <div class="d-grid">
            <button type="submit" class="btn btn-primary btn-lg rounded-pill">
              Envoyer le potin
            </button>
          </div>

        </div>
      </div>
    </div>
  </div>
</form>