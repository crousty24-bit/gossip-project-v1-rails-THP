<div class="container mt-4">
  <!-- Jumbotron moderne -->
  <div class="p-5 mb-5 rounded shadow text-center" 
      style="background-color: #FFF9F0; color: #000; font-family: 'Poppins', sans-serif;">
    <!-- Image Hero Logo Gossip (à modifier) -->
    <%= link_to image_tag("Gossip_Project-logo.png", alt: "Logo", height: 200)%>
    <h1 class="display-4 fw-bold">Bienvenue <%= @first_name %> !</h1>
    <p class="lead mt-3">Voici les derniers potins du site. Clique sur le bouton ci-dessous pour créer le tiens</p>
    <%= link_to "Créer mon potin", new_gossip_path, class: "btn btn-primary" %>
  </div>

  <!-- Liste des gossips -->
  <div class="row g-4">
    <% @gossips.each do |gossip| %>
      <div class="col-sm-12 col-md-4">
        <%= link_to gossip_path(gossip), class: "text-decoration-none" do %>
          <div class="card shadow-sm border-0 h-100">
            <div class="card-header" style="background-color: #FFD8A8; color: #333;">
              <h5 class="mb-0"><%= gossip.title %></h5>
            </div>
            <div class="card-body">
              <span class="badge mb-2" style="background-color: #FFE6CC; color: #555;">
                Auteur : <%= gossip.user.first_name %>
              </span>
              
              <span class="badge ms-1 <%= gossip.comments.count.zero? ? 'bg-secondary' : 'bg-success' %>">
                💬 <%= gossip.comments.count.zero? ? 'Premier à commenter' : "#{gossip.comments.count} commentaire#{gossip.comments.count > 1 ? 's' : ''}" %>
              </span>
              
              <p class="card-text mt-2" style="overflow: hidden;">
                <%= gossip.content %>
              </p>
            </div>
            <div class="card-footer bg-light text-end">
              <small class="text-muted">Voir plus →</small>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
